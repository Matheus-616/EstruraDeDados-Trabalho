#include "Fila.h"
#include <stdlib.h>
#include <stdio.h>


int main() {
    //Fila F;
    //int erro;
    char c[20],v[10];
    strcpy(c,"ola");
    strcpy(v,"ola");
    if (!strcmp(c,v)) printf("comparação funcionou\n");
    else printf("sim\n");
    /*
    Cria(&F);
    printf("%d \n", Tamanho(&F));
    Push(&F, "dada", &erro);
    if (erro==1) printf("erro no dada \n");
    Push(&F, "dede", &erro);
    if (erro==1) printf("erro no dede \n");
    Push(&F, "didi", &erro);
    if (erro==1) printf("erro no didi \n");

    printf("%d \n", Tamanho(&F));

    Pop(&F, c, &erro);
    printf("%s \n", c);

    Pop(&F, c, &erro);
    printf("%s \n", c);

    printf("%d \n", Tamanho(&F));

    Pop(&F, c, &erro);
    printf("%s \n", c);

    printf("%d \n", Tamanho(&F));

    Finaliza(&F);*/

    return(0);
}